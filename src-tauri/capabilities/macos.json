{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "macos-only",
  "description": "macOS-specific permissions",
  "windows": ["main", "camera-overlay"],
  "platforms": ["macOS"],
  "permissions": [
    "macos-permissions:default"
  ]
}
